{
  "name": "archmage",
  "version": "1.1.0",
  "description": "Archmage — a pure arcade roguelike. Thirteen elements, seventy-eight resonances, five shuffled tyrants, adaptive procedural music. Version 1.1 — True Direction.",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build && node -e \"require('fs').cpSync('.next/static', '.next/standalone/.next/static', {recursive: true}); require('fs').cpSync('public', '.next/standalone/public', {recursive: true});\"",
    "start": "NODE_ENV=production bun .next/standalone/server.js",
    "build:pages": "BUILD_MODE=pages next build",
    "brand": "node scripts/build-brand.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "next": "^16.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sharp": "^0.34.3",
    "tailwind-merge": "^3.3.1",
    "zod": "^4.0.2",
    "zustand": "^5.0.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "bun-types": "^1.3.4",
    "eslint": "^9",
    "eslint-config-next": "^16.1.1",
    "tailwindcss": "^4",
    "tw-animate-css": "^1.3.5",
    "typescript": "^5"
  }
}
